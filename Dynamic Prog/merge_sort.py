# def merge_sort(unsorted_numbers, start, end):
#     if start < end:
#         mid = int((start + end) / 2)
#         merge_sort(unsorted_numbers, start, mid)
#         merge_sort(unsorted_numbers, mid + 1, end)
#         merge(unsorted_numbers,start, mid, end)
#
# def merge(date, start, mid, end)
#
#
#
#
# num_arr = [2, 1, 5, 10, 9, ]
# merge_sort(num_arr, 0, len(num_arr) - 1)
#
