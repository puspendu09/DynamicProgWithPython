# class Solution:
#     # @param A : list of integers
#     # @param b : integers
#     # @return A : a list of integers
#     @staticmethod
#     def solve(self, A, B):
#         i = 0
#         _max = len(A)
#         A_to_dict = {x: i for i, x in enumerate(A)}
#
#         while B and i < len(A):
#             j = A_to_dict[_max]
#             if
#
#         return A
